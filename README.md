# Secure Login System with Attack Prevention

A secure login system built in Java that protects against common authentication attacks.

## What it does
- Lets users register and log in securely
- Stores passwords using hashing (never plain text)
- Tracks failed login attempts per user
- Locks the account temporarily after too many failed attempts

## Tech Stack
- Java
